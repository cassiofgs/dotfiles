- defaults:
    link:
        relink: true

- clean: ['~']

- link:
    ~/.bashrc: bashrc
    ~/.vimrc: vimrc
    ~/.tmux.conf: tmux.conf

- shell:
    - [git submodule update --init --recursive, installing submodules]

